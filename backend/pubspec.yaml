name: biteplate_backend
description: BitePlate Smart Restaurant Management System - Dart backend (REST API + OOP design patterns).
version: 1.0.0
environment:
  sdk: ^3.4.0

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
